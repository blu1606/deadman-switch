# 10.2 Multi-Media Bundle

> Hỗ trợ thêm nhiều loại nội dung vào vault.

## Goal
User có thể thêm nhiều items thay vì chỉ 1 file:
- 📝 Text notes
- � Files (PDF, docs...)
- 🎤 Voice recordings
- 🖼️ Images
- 🎬 Videos

## Approach
**Bundle Pattern** - Gom nhiều items thành 1 package JSON, encrypt, upload IPFS → vẫn chỉ 1 CID on-chain.

```
Items[] → Bundle JSON → Encrypt → IPFS → 1 CID
```

**Không cần contract change.**

## Bundle Schema
```
{
  version: 1,
  items: [
    { type, name, mimeType, size, data (base64) }
  ],
  metadata: { totalSize, itemCount }
}
```

## Limits
| Limit | Value |
|-------|-------|
| Max items | 20 |
| Max per file | 50 MB |
| Max total | 100 MB |

## New Components
1. **VaultContentEditor** - UI để add/remove/reorder items
2. **VoiceRecorder** - Record audio bằng MediaRecorder API
3. **VaultItemCard** - Hiển thị 1 item với preview/delete
4. **ItemTypeButtons** - Buttons để add từng loại

## Flow Changes
- Create: Thêm step "Add Content" với multi-item editor
- Claim: Render danh sách items với download buttons cho từng item
