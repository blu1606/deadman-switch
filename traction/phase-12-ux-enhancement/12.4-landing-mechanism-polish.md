# 12.4 Landing Mechanism Polish (Pulse Upgrade)

## 🎯 Goal
Refine the "Digital Fire Alarm" (Mechanism) section to be visually cohesive and interactive, replacing generic emojis with crisp Lucide icons and adding a "Pulse" animation to reinforce the "Alive/Dead" concept.

## 🎨 Visual Design
*   **Theme:** "The Digital EKG" - A unifying line that connects all steps, which pulses like a heartbeat.
*   **Icons:** Consistent Lucide React icons instead of emojis.
*   **Interactivity:** Hovering a step triggers a specific "Kip Reaction" (Tooltip/Popover).

## 🛠️ Implementation

### 1. Icon Replacement
Replace the current emojis in `EmergencySwitch.tsx` with:
- **Load:** `ShieldCheck` (Security) or `Vault` (if available, otherwise `Box`)
- **Timer:** `Timer` or `Hourglass`
- **Heartbeat:** `Activity` (The classic EKG pulse line)
- **Release:** `UnlockKeyhole` (Unlocking)

### 2. The "Pulse" Line
Replace the simple gradient div with a `PulseLine` component:
- **SVG Path:** A straight line that has a "blip" (EKG spike) traveling across it.
- **Animation:** Use `framer-motion` to animate the `strokeDashoffset` or move a glowing dot along the path.

### 3. Kip Interaction (Quick Win)
Add a simple generic `KipPopover` or just conditional rendering:
- **Hover Step 1:** Kip showing a shield (Happy).
- **Hover Step 2:** Kip looking at a watch (Waiting).
- **Hover Step 3:** Kip checking a pulse (Focused).
- **Hover Step 4:** Kip handing over a letter (Salute).

## ✅ Action Items
- [ ] Install/Import Lucide icons (`ShieldCheck`, `Timer`, `Activity`, `UnlockKeyhole`).
- [ ] Update `EmergencySwitch.tsx` steps configuration.
- [ ] Create simple SVG Pulse Animation.
- [ ] (Optional) Add `KipReact` tooltip on hover.
